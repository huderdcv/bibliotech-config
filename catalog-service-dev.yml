server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bibliotech-catalog-db
    username: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
      show-sql: true
